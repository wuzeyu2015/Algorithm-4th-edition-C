#pragma once

#include "stdafx.h"

string multiply(string num1, string num2) {
	string res;

}

void test_110() {
	TreeNode* p = new TreeNode(0);
	p->left = NULL;
	p->right = NULL;
	string s = multiply("33", "33");
}